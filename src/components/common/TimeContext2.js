// This is for class implementation of Show ETH page

import React from "react";

const TimeContext2 = React.createContext();

export const TimeProvider = TimeContext2.Provider;
export const TimeConsumer = TimeContext2.Consumer;

export default TimeContext2;
